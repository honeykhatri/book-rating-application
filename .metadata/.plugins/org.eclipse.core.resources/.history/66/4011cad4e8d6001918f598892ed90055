server:
  port: 8081
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/honeykhatri/application-config/
  application:
    name: config
  security:
    user:
      name: configUser
      password: configPassword
      roles:
      - SYSTEM
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    region: default
    registry-fetch-interval-seconds: 5
    service-url:
      defaultZone: http://discUser:discPassword@localhost:8082/eureka/
